import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs/blocks'

import { Node } from './Node.component'

<Meta title='Board/Parts/Node' component={Node} />

# Node

<Preview>
  <Story name='Node'>
    <Node chip='blue' player='red' />
  </Story>
</Preview>

# Node hover options

<Preview>
  <Story name='Node hover options'>
    <div style={{ height: '75px' }}>
      <Node chip='empty' player='red' />
      <Node chip='empty' player='red' />
    </div>
    <div>
      <Node chip='blue' player='red' />
      <Node chip='red' player='red' />
    </div>
    <div style={{ height: '75px' }}>
      <Node chip='empty' player='blue' />
      <Node chip='empty' player='blue' />
    </div>
    <div>
      <Node chip='blue' player='red' />
      <Node chip='red' player='red' />
    </div>
  </Story>
</Preview>

## Controlled Node

export const Template = ({ chip, ...args }) => <Node {...args} chip={chip} />

<Story
  name='Controlled Node'
  args={{
    chip: 'empty',
    player: 'red',
  }}>
  {Template.bind({})}
</Story>

<ArgsTable story={'Controlled Node'} />
