import { ArgsTable, Meta, Preview, Story } from '@storybook/addon-docs/blocks'
import { BoardUI } from './Board.component'
import { createBoardState } from '../../store/game/logic/createBoardState'

<Meta title='Board/Example Board' component={BoardUI} />

# Board

export const boardState = createBoardState(4)
boardState[1][3] = 'blue'
boardState[2][1] = 'red'
boardState[2][2] = 'blue'
boardState[2][3] = 'red'
boardState[3][0] = 'red'
boardState[3][1] = 'blue'
boardState[3][2] = 'red'
boardState[3][3] = 'red'

<Preview>
  <Story name='Board'>
    <BoardUI board={boardState} player={'blue'} />
  </Story>
</Preview>

## Controlled Board

export const Template = (args) => <BoardUI {...args} />

<Story
  name='Controlled Board'
  args={{
    nodeSize: 20,
    board: boardState,
  }}>
  {Template.bind({})}
</Story>

<ArgsTable story={'Controlled Board'} />
